# macOS app

Not started.

The Mac app is the menu bar front end for the bridge: it starts and stops the server, shows
which projects and sessions exist, displays the pairing code, and lists registered devices.
It is a view onto the bridge process rather than a separate source of truth.
