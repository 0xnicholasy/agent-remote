# Relay

Not started.

An optional internet relay for the case where the watch and the Mac are on different
networks. It is intended to forward already encrypted envelopes without being able to read
them, and it is deliberately the last connectivity option rather than the default.
